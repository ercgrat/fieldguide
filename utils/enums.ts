export enum QueryKey {
  User = 'User',
  Login = 'Login',
  Organization = 'Organization'
}

export enum Route {
  Home = '/home',
  Login = '/',
  Onboarding = '/onboarding'
}

export enum HttpResponseHeader {
  Error = 'FIELD_GUIDE_ERROR'
}

export enum HttpMethod {
  GET = 'GET',
  POST = 'POST',
  PUT = 'PUT',
  PATCH = 'PATCH',
  DELETE = 'DELETE'
}
